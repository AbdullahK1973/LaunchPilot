import { NextResponse } from "next/server";import {requireUser} from "@/lib/auth";import {getDb} from "@/lib/db";
export async function GET(){const user=await requireUser();const data=await getDb().user.findUnique({where:{id:user.id},omit:{passwordHash:true},include:{memberships:{include:{workspace:{include:{brands:{include:{products:true}},launches:{include:{outputs:{include:{revisions:true}}}}}}}}}});return new NextResponse(JSON.stringify(data,null,2),{headers:{"content-type":"application/json","content-disposition":'attachment; filename="launchpilot-data.json"',"cache-control":"no-store"}})}
