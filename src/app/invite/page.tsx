import { acceptInvitation } from "@/app/actions/workspace";import {Button} from "@/components/ui/button";import {Card} from "@/components/ui/card";
export default async function InvitePage({searchParams}:{searchParams:Promise<{token?:string}>}){const {token}=await searchParams;return <main className="grid min-h-screen place-items-center bg-slate-50 p-6"><Card className="max-w-md p-8"><h1 className="text-2xl font-semibold">Workspace invitation</h1><p className="my-4 text-slate-600">Accept this invitation using the account with the invited email address.</p><form action={acceptInvitation}><input type="hidden" name="token" value={token||""}/><Button disabled={!token}>Accept invitation</Button></form></Card></main>}
