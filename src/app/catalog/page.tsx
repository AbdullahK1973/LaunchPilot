import { DashboardShell } from "@/components/layout/dashboard-shell";import {Card} from "@/components/ui/card";import {Button} from "@/components/ui/button";import {Input,Select} from "@/components/ui/input";import {Textarea} from "@/components/ui/textarea";import {requireWorkspace} from "@/lib/auth";import {getDb} from "@/lib/db";import {createBrand,createLaunchFromProduct,createProduct} from "@/app/actions/catalog";
const Field=({label,children}:{label:string,children:React.ReactNode})=><label><span className="mb-1 block text-sm font-semibold">{label}</span>{children}</label>;
export default async function CatalogPage(){const {workspace}=await requireWorkspace();const brands=await getDb().brand.findMany({where:{workspaceId:workspace.id},include:{products:true},orderBy:{name:"asc"}});return <DashboardShell title="Brand and product catalog" description="Reuse product context across launches."><div className="mx-auto max-w-6xl space-y-6"><div className="grid gap-6 lg:grid-cols-2"><Card className="p-6"><h2 className="text-xl font-semibold">Add brand</h2><form action={createBrand} className="mt-4 space-y-3"><Field label="Name"><Input name="name" required/></Field><Field label="Tone"><Input name="tone" defaultValue="Premium" required/></Field><Field label="Audience"><Textarea name="targetAudience" minLength={10} required/></Field><Field label="Default launch goal"><Textarea name="launchGoal" minLength={10} required/></Field><Button>Save brand</Button></form></Card><Card className="p-6"><h2 className="text-xl font-semibold">Add product</h2>{brands.length?<form action={createProduct} className="mt-4 space-y-3"><Field label="Brand"><Select name="brandId">{brands.map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</Select></Field><div className="grid grid-cols-2 gap-3"><Field label="Name"><Input name="name" required/></Field><Field label="Category"><Input name="category" required/></Field></div><Field label="Description"><Textarea name="description" minLength={10} required/></Field><Field label="Key features"><Textarea name="keyFeatures" required/></Field><Field label="Price"><Input name="price" required/></Field><Button>Save product</Button></form>:<p className="mt-4 text-slate-600">Create a brand first.</p>}</Card></div><Card className="p-6"><h2 className="text-xl font-semibold">Catalog</h2><div className="mt-4 grid gap-4 md:grid-cols-2">{brands.flatMap(b=>b.products.map(p=><div key={p.id} className="rounded-lg border p-4"><p className="text-xs font-bold uppercase text-sky-700">{b.name}</p><h3 className="mt-1 text-lg font-semibold">{p.name}</h3><p className="text-sm text-slate-500">{p.category} · {p.price}</p><form action={createLaunchFromProduct} className="mt-4"><input type="hidden" name="productId" value={p.id}/><Button variant="secondary">Create launch</Button></form></div>))}</div></Card></div></DashboardShell>}
