import Link from "next/link";import {verifyEmail} from "@/app/actions/account";import {Button} from "@/components/ui/button";import {Card} from "@/components/ui/card";
export default async function VerifyEmailPage({searchParams}:{searchParams:Promise<{token?:string;invalid?:string}>}){const {token,invalid}=await searchParams;return <main className="grid min-h-screen place-items-center bg-slate-50 p-6"><Card className="w-full max-w-md p-8"><h1 className="text-2xl font-semibold">Verify your email</h1>{invalid?<p className="my-4 text-rose-700">This verification link is invalid or expired.</p>:<p className="my-4 text-slate-600">Confirm this email address to finish securing your account.</p>}{token&&!invalid?<form action={verifyEmail}><input type="hidden" name="token" value={token}/><Button>Verify email</Button></form>:<Link className="font-semibold text-sky-700" href="/login">Back to sign in</Link>}</Card></main>}
